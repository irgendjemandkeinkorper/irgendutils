# obsidian-vault-forge needs no secrets — it writes plain Markdown files and
# never talks to a network or a database. This file exists only to satisfy the
# repo convention that every app ship a .env.example.
#
# NO_COLOR=1   # disable ANSI color in output (also: --no-color)
