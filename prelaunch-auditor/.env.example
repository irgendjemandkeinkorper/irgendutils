# Basic-auth credentials for auth-gated staging sites (used by the live adapter).
# Leave empty for public sites. Never commit a real .env.
AUDIT_HTTP_USER=
AUDIT_HTTP_PASS=

# Set to any value to disable ANSI colors in console output (standard convention).
# NO_COLOR=1
