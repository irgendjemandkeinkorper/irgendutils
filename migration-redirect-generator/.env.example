# Example Environment File
# (No critical secrets needed, but HTTP verification can respect rate limiting/user agents here)

HTTP_VERIFY_TIMEOUT_MS=5000
HTTP_USER_AGENT="Mozilla/5.0 (compatible; MigrationRedirectGenerator/1.0)"
