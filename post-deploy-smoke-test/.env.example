# Secrets for authed smoke checks. Copy to .env (never commit .env).
# The variable NAME is whatever each authed check's `app_password_env` says;
# WP_APP_PASSWORD is the default. Value is a WordPress Application Password
# (Users -> Profile -> Application Passwords).
WP_APP_PASSWORD="abcd efgh ijkl mnop qrst uvwx"
